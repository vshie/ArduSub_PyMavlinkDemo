version: '3.8'

services:
  ardusub-control:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=main.py
      - FLASK_ENV=development
    volumes:
      - ./app:/app
    restart: unless-stopped
    networks:
      - ardusub-network

networks:
  ardusub-network:
    driver: bridge
